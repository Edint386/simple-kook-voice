from setuptools import setup

setup(
    name='kookvoice',
    version='0.0.1',
    author='Edint386',
    description='kook music python sdk',
    packages=['kookvoice'],
    install_requires=[
        'enum'
    ],
)
